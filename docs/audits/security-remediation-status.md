# Security remediation status

Last updated: 2026-09-09

Remediation checkout: `/tmp/ragz-security-remediation-20260908`

Baseline: `3fac9fb1d02c9327f243418ebbb905466c8bcef5` (PR #11 head), whose merge base is `9d08839f6855967f3b141b731a269a54b76222fb` (current `origin/main`). The dirty shared checkout at `90bb3b1` is preserved and is not an implementation base. CAG remains active development owned by its existing developer; this branch does not take over response-cache integration.

Status terms: **investigating** means the audit is a lead being rechecked on the target revision; **confirmed** means the vulnerable/correctness invariant is established on that revision; **fixed** requires failing-before and passing-after evidence plus review; **already fixed** and **inapplicable** require revision-specific evidence; **blocked** names a concrete external dependency.

## Application findings

| ID | Target revision | Owner | Planned invariant | Status | Patch and verification |
|---|---|---|---|---|---|
| SEC-001 | `3fac9fb` | Sol | Server-derived preview policy; active/unknown content cannot execute in the app origin; escaped text and sandboxed safe frames preserve PDF/PPTX/OCR/large-file behavior and legacy handling. | Fixed; reviewed and verified | Red: API accepted MIME-spoofed PDF and returned HTML inline; four viewer regressions failed. Patch: `file_types.py`, streaming file response, narrow React renderers. Green: 30 focused backend tests, 21 focused frontend tests, and Playwright's three harmless HTML/SVG/text/PDF cases. Independent first review's charset, cache, streaming, signature, Unicode and real-browser findings were corrected. |
| SEC-002 | `3fac9fb` | Sol | Stream attachment transfer and bound rate, concurrent/pending count, aggregate bytes, parser/page/decompression work; reject before storage/enqueue. | Fixed; reviewed and verified | Red: route accumulated bytes and had no aggregate/pending admission. Patch: spooled streaming upload, per-user rate gate, PostgreSQL reservations, count/byte/pending caps and pre-storage archive/PDF checks. Parallel/rejection/failure tests pass with storage/enqueue spies. |
| SEC-003 | `3fac9fb` | Sol | Sensitive audit/ACL-bypass grants require independent grant authority; deny self-escalation and cross-org grants while preserving delegated administration. | Fixed; reviewed and verified | Red: retained audit proof and old service tests allowed same-user sensitive overlay. Patch: `roles.sensitive.assign` and sensitive grant checks in `tenancy/service.py`. Green service/API coverage denies self/cross-org grants and permits independently delegated/superadmin grants. |
| SEC-004 | `3fac9fb` | Sol | One egress address policy rejects unintended non-global/shared IPv4 and IPv6 on direct requests and redirects while preserving explicit operator-controlled internal integrations. | Fixed confirmed gap; conditional admin pinning remains | Red: retained CGNAT redirect reproducer reached synthetic content. Patch: central `not is_global` address policy reused by media/web, mapped IPv4 handling, and explicit operator CIDRs that cannot allow local/metadata ranges. Fourteen direct/redirect IPv4/IPv6 tests pass. Admin connectors still re-resolve after validation; see conditional disposition. |
| SEC-005 | `3fac9fb` | Sol | Passive auth failure and identity change cancel and clear/partition sensitive query state; stale A responses cannot populate B's cache. | Fixed; reviewed and verified | Red: refresh failure/login only changed token/navigation while the global query client survived. Patch: cancel then remove all identity-bound queries on passive failure, refresh identity restore, login/register and logout. Green A-expiry -> B-login test resolves a late A request and renders only B; 36 focused auth/client/document tests passed. |
| SEC-006 | `3fac9fb` | Sol | External attachment object/vector identifiers enter durable idempotent cleanup work before relational cascade; failures retry; legacy orphan recovery is non-destructive and explicit. | Fixed; reviewed and verified | Red: retained cascade proof removed the only blob/vector identifiers. Patch: no-FK `attachment_cleanup_jobs`, before-cascade scheduling, retry worker and read-only `scripts/inventory_attachment_orphans.py`. Three isolated lifecycle/failure/idempotency/dry-run tests pass; no cleanup ran against existing data. |
| SEC-007 | `3fac9fb` | Sol | Permanent and attachment quota admission atomically reserves in-flight count/bytes/work and finalizes or releases exactly once under concurrent uploads. | Fixed; reviewed and verified | Red parallel document requests both returned 201 at a one-object boundary. Patch: per-org PostgreSQL advisory lock plus expiring durable reservations, commit/finalize/release and storage compensation. Parallel count/byte, rejection-before-storage/enqueue, failure and cancellation tests pass. The 1 GiB streamed document boundary remains. |
| SEC-008 | `3fac9fb` | Sol | Credential lifecycle mutation and safe audit attribution commit atomically; transaction failure leaves neither partial state nor credential material in logs. | Fixed; reviewed and verified | Red: API-key service committed create/revoke before route audit. Patch stages lifecycle, audit and one route commit. Injected audit failures roll back create/revoke; API-key/auth focused tests pass and raw credentials remain one-time only. |
| SEC-009 | `3fac9fb` | Sol | Authentication denials persist `result=denied` with safe reason, method and trusted request attribution. | Fixed; reviewed and verified | Red retained proof stored password denial as success. Password and OIDC conflict/domain/inactive denials now store denied result, bounded reason code, auth method, trusted client IP and internal/unknown target identity. Focused persisted-field tests pass without supplied email/credential logging. |
| SEC-010 | `3fac9fb` | Luna inventory / Sol integration | CI audits the explicit frozen application inventory and asserts representative packages are present; runtime and build trees are separate gates. | Fixed; cross-check refreshed | Workflow exports 218 runtime and 258 full/build requirements and audits their hashes. Current runs pass while explicitly reporting one reviewed Accelerate exception; no other advisory is suppressed. Frontend production/full inventories are clean. |
| SEC-011 | `3fac9fb` | Luna inventory / Sol integration | Compatible vulnerable packages and locks are updated after reachable-path assessment; no disabled or blanket-ignored audit. | Compatible findings fixed; one no-release exception | Transformers is 5.15.1, js-yaml 4.3.2 and Vitest 4.1.11. Accelerate 1.14.0 is the latest release with no patched version; affected checkpoint APIs are not reachable in Ragz/installed dependency paths. CI carries only exact `CVE-2026-69112`, dated for review/removal. Docker Scout/backend-image limits remain. |
| SEC-012 | `3fac9fb` | Sol | Every completed billable provider action records idempotent durable usage across later generation/tool/cancellation failures without duplicate charging. | Fixed; reviewed and verified | Red: agent totals existed only after final gather; additional red tests lost completed expansion on Q1 failure and completed comparison generation on cancellation. Patch: partial unique idempotency key plus independent shielded session writes per planner, billable web call, retrieval provider stage and generation. Mocked provider/real PostgreSQL regressions pass and Q1 survives optional alternative failure. |
| LOCAL-001 | `90bb3b1` local; compare `9d08839`/`3fac9fb` | Sol | Preserve pending/failed projection exclusion, CAS activation, outbox and tenant constraints; direct search and chat both reject a completed-revision race if reproducible. | Confirmed bounded direct-search gap fixed | Deterministic direct test reproduced an old Qdrant response after projection completed; chat's later source authorization already rejected its stale source. Patch stamps Qdrant payloads with `security_revision`, rechecks current authorized revisions and moves legacy indexed rows pending until restamped. Direct completed race, older pending race and chat source recheck pass. No cross-tenant leak beyond the bounded reproduced stale-revision condition is claimed. |

## PR #11 findings

| ID | Target revision | Owner | Planned invariant | Status | Patch and verification |
|---|---|---|---|---|---|
| PR-001 | `3fac9fb` | Sol | Both query-derived caches always remove/resolve owner reservations through compute, lock reacquisition and publication cancellation; waiters retry and unrelated keys progress. | Fixed; reviewed and verified | Retained reproducer reported dead slots in both caches. Shielded publication tasks now resolve/remove owner futures after caller cancellation. Five deterministic tests cover compute/publication locks, multiple waiters and unrelated keys. |
| PR-002 | `3fac9fb` | Sol | Alternative embedding failure degrades to usable Q1 and retains exactly-once usage for completed expansion/embedding work; cancellation still propagates. | Fixed; reviewed and verified | Mocked second embedding call fails after billed Q1/expansion; result stays usable with query_count=1 and exact durable rows. Cancellation is not caught as fallback. |
| PR-003 | `3fac9fb` | Sol | Comparison retrieval usage commits before fallible source lookup and survives lookup/cancellation without duplicate charging. | Fixed; reviewed and verified | Comparison commits legacy staged retrieval before `_sources`; retrieval's new rows are independently durable. Source-failure and generation-cancellation tests inspect committed usage through a real isolated database. |
| PR-004 | `3fac9fb` | Sol | Grounding/no-answer uses exactly the eligible post-recheck candidate/revision set used for generation. | Fixed; reviewed and verified | Added high-score removed + low-score allowed regression. Filter/probes are rebuilt after recheck, removed content is absent and no-answer remains true; focused tests pass. |

## 2026-09-09 independent cross-check

| ID | Reviewed revision | Disposition | Patch and verification |
|---|---|---|---|
| CC-001 | `ed2403f` | Fixed | Cleanup jobs retain `user_id`/`size_bytes`; incomplete jobs count toward org/user count and bytes until external cleanup completes. The independent quota test failed before and passes after; lifecycle coverage verifies failed retry and successful release. |
| CC-002 | `ed2403f` | Fixed | Upload finalization rereads the exact durable row after ambiguous commit failure and deletes externally only on a definite negative. Committed-then-cancelled attachment/document regressions pass; the reviewer acceptance changed from failure to pass. |
| CC-003 | `ed2403f` | Fixed after independent rereview | Auth generations reject stale refresh before network and scope XHR upload retries. Login/register/logout abort and settle older work; password/OIDC login revoke a presented different-user family before committing the new identity while preserving supported same-user sessions. Late-response, upload, cross-user family and session-inventory tests pass. |
| CC-004 | `ed2403f` | Fixed | The broken sandboxed native PDF iframe is replaced by PDF.js worker/canvas rendering. Chromium renders non-empty pixels from the valid repository fixture with no iframe; active HTML/SVG and literal-text tests still pass. |
| CC-005 | `ed2403f` | Compatible fixes complete; exact no-release residual | js-yaml 4.3.2, Vitest/@vitest/mocker 4.1.11 and Node-20-compatible PDF.js 5.4.624 make frontend production/full audits clean. Accelerate 1.14.0 has no fixed release and affected loaders are absent from Ragz/installed dependency paths. CI reports one exact exception and executes a hard 2026-10-15 expiry gate. |
| CC-006 | `ed2403f` | Fixed after independent rereview | Usage records precede expansion publication, Redis counters and local embedding validation. Real LiteLLM errors carry billed tokens from the current malformed-width response and earlier successful batches; the caller durably records them before rethrow. Default expansion performs one recorder call. |
| CC-007 | `ed2403f` | Fixed after independent rereview | TEI/LiteLLM expected HTTP, JSON, data-shape and usage-shape failures normalize to `UpstreamError`; cancellation and unrelated faults remain visible. Adapter plus Q1 fallback tests pass. |
| CC-008 | `ed2403f` | Fixed | Dense no-answer scores count only when exact document/revision/page/chunk identity is present in the final generation set. A 0.99 probe-only document plus 0.1 eligible evidence yields `no_answer=true`. |

These rows supersede the earlier patch descriptions where the cross-check required
an additional boundary fix; the original failing evidence remains historical.

## Existing PR review comments

| # | Topic | Target revision | Owner | Required disposition | Status / evidence |
|---:|---|---|---|---|---|
| 1 | Isolation test can pass without expansion | `3fac9fb` | Luna test inventory / Sol review | Prove expansion ran and include a same-tenant positive control. | Fixed: invocation, same-tenant positive and rival exclusion assertions pass. |
| 2 | Invalid/disabled default model assignment and fallback | `3fac9fb` | Sol | Reject invalid assignments, define existing-invalid fallback, preserve ordinary chat. | Fixed: assignment uses enabled chat-model resolution; explicit invalid stays an error, a legacy invalid non-null default falls back to the first enabled chat model, and no configured model remains a conflict. |
| 3 | Streaming cancellation loses paid web usage | `3fac9fb` | Sol | Covered by SEC-012 with per-completed-call durability. | Fixed with per-planner/per-billable-web run-scoped durable rows before frames/final aggregation. |
| 4 | Alternative embedding fallback | `3fac9fb` | Sol | Covered by PR-002. | Fixed; Q1 remains usable and completed expansion/Q1 usage persists. |
| 5 | Removed candidate affects no-answer | `3fac9fb` | Sol | Covered by PR-004. | Fixed for newly unprojected and completed revision-mismatch removal; rerun probes exclude exact dropped ids. |
| 6 | Async workspace lookup in E2E | `3fac9fb` | Luna bounded edit / Sol review | Await lookup completion before create-or-reuse decision. | Fixed: both specs wait for the switcher's product loading state before deciding. Full-stack E2E remains environment-dependent. |
| 7 | ADR claims one dense batch | `3fac9fb` | Luna bounded edit / Sol review | Describe actual Q1-plus-alternatives batching or change code consistently. | Fixed in ADR-0007: Q1 starts first and alternatives form a second bounded batch. |
| 8 | ADR query-count inconsistency | `3fac9fb` | Luna bounded edit / Sol review | Distinguish production maximum/default from internal/evaluation overrides. | Fixed in ADR-0007: production total is three; internal/evaluation override supports one, three or five. |
| 9 | Stale client role gating | `3fac9fb` | Sol | Use current server-derived authorization for visibility; backend remains authority. | Fixed for the reviewed settings control using server authorization; demoted stale-token regression passes. This remains a UX consistency issue, not an auth exploit. |
| 10 | Cache publication cancellation | `3fac9fb` | Sol | Covered by PR-001. | Fixed; retained reproducer reports no dead slots and billing completes before cache publication. |
| 11 | HTTP-date `Retry-After` | `3fac9fb` | Luna bounded edit / Sol review | Parse delta-seconds and HTTP-date, clamp to configured cap. | Fixed: numeric/date/past/future mocked regressions pass with a 30-second cap. |
| 12 | Comparison source lookup precedes usage commit | `3fac9fb` | Sol | Covered by PR-003. | Fixed; source-failure regression observes durable retrieval usage. |
| 13 | Full 32k expansion input | `3fac9fb` | Sol | Bound expansion input deliberately and record intended truncation/cost behavior without misclassifying it as an auth exploit. | Fixed as cost hardening: expansion input caps at 4,000 characters; chat's 32k product input remains unchanged. |
| 14 | Evals selects unavailable workspace default | `3fac9fb` | Luna bounded edit / Sol review | Submit only an available enabled chat model and handle an empty set. | Fixed: available-model fallback and empty disabled state pass frontend tests. |
| 15 | Mutable “Fixed input” result labels | `3fac9fb` | Luna bounded edit / Sol review | Snapshot submitted question/model with each result. | Fixed: results store the submitted question/model; edit-after-completion regression passes. |

## Conditional observations and policy dispositions

| Observation | Target revision | Owner | Required disposition | Status / evidence |
|---|---|---|---|---|
| Bot audience | `3fac9fb` | Sol | Confirm product policy and enforcement boundary; do not publish private corpora or silently change service-account semantics. | Conditional, no policy change: signatures authenticate the platform, while messages intentionally execute as the configured least-privilege integration user. No deployment audience/channel policy was supplied, so no external-user leak was confirmed. Deployment must restrict audience or define sender binding before exposing a private corpus. |
| Historical answers after revocation | `3fac9fb` | Sol | Record current prospective/snapshot policy and avoid an unrelated retention change. | Conditional product policy: snapshots remain visible only to the chat owner after workspace membership checks. No cross-user path was shown; retention was not silently changed. Retrospective revocation would require separate migration and UX work. |
| Global query-derived cache sharing | `3fac9fb` | Sol | Partition by tenant/principal or explicitly accept/document privacy and attribution tradeoff. | Fixed: embedding and expansion keys/single-flight ownership include the organization in the opaque hash. Distinct from CAG response caching. |
| Comparison forces MQR | `3fac9fb` | Sol | Reconcile the request-scoped comparison cost with intended permissions; do not call it a persistent-setting bypass without evidence. | Intended evaluation behavior: the route requires `evals.run`, compares native single-query with an explicit multi-query arm, and never mutates the workspace toggle. Provider calls use normal quota checks and durable accounting. No auth exploit was established; ADR-0007 documents the internal/evaluation override. |
| Admin egress | `3fac9fb` | Sol | Preserve intended OIDC/SMTP internal integrations through explicit operator policy; assess DNS-pinning gap separately. | Partially hardened: explicit private CIDRs apply only to operator OIDC/SMTP/model targets and cannot permit local/link-local/metadata; persisted model targets are revalidated before proxy replay. Admin clients still check then re-resolve, so DNS TOCTOU remains conditional and documented. |
| Legacy attachment/upload orphans | `3fac9fb` | Sol | Supply safe inventory/reconciliation tooling without running it against existing data. | Fixed operational seam: `scripts/inventory_attachment_orphans.py` emits a read-only DB/job/MinIO/Qdrant comparison. No existing data was scanned or deleted. Historical sizes are conservatively backfilled to the former 50 MiB ceiling. |
| API-key prefix collision | `3fac9fb` | Sol | Increase lookup entropy or make collision lookup unambiguous while preserving credential secrecy and compatibility. | Fixed: new prefixes use 24 characters; lookup checks 24/legacy-12 candidates and constant-time full hashes without collision exceptions. |
| Citation navigation schemes | `3fac9fb` | Sol | Apply consistent HTTP(S) validation as hardening if gap remains. | Fixed: source/chat rendering accepts only HTTP(S), renders other schemes inert and tolerates malformed persisted URLs. |
| Nested/provider logging redaction | `3fac9fb` | Sol | Confirm whether sensitive nested/exception values can cross logs; fix only demonstrated unsafe paths. | Concrete provider-body gaps fixed: LLM/embedding upstream errors retain status only; summary/enrichment warnings record lengths rather than model text. Marker regressions fail before and pass after. Optional Sentry was not exercised. |
| Image/build assurance | `3fac9fb` | Luna inventory / Sol integration | Separate package/build/image evidence; avoid unsupported production claims. | Frontend image and backend wheel/sdist build pass; nginx syntax passes with an isolated `api` mapping. Docker Scout scan required an unavailable Docker login. Backend image was not built/scanned, so no whole-image assurance claim is made. |

## Patch map and compatibility

| Concern | Primary patch locations | Migration / compatibility impact |
|---|---|---|
| Preview and browser state | `documents/file_types.py`, document route/storage stream, `document-file.ts`, `pdf-preview.tsx`, viewer drawer, auth query-state helper, Playwright preview harness | Legacy active/unknown files become download-only. PDF.js renders the requested page to a worker-backed canvas without a document iframe. Text preview is capped at 2 MiB; ACL-sensitive blobs are evicted on unmount/change. |
| Resource admission and parsing | `quotas/resource_admission.py`, document/attachment services and routes, ASGI/nginx body caps, path-based parsers, worker checks | Finite defaults: 10,000 documents/100 GiB per org; attachment count/bytes/pending/rate/page/archive/character/pixel caps. Documents still allow streamed 1 GiB uploads; text has a 32 MiB parser cap. LiteParse preserves complete bounded page ranges and PPTX/Docling/OCR paths remain available. |
| External cleanup | `chat/cleanup.py`, chat/attachment deletion, maintenance worker, `scripts/inventory_attachment_orphans.py` | Durable no-FK jobs retain user/byte allocation, survive cascades and retry idempotently. Quota releases only after completion. Legacy inventory is read-only. |
| Authority, audit and egress | tenancy permissions/service, auth/API-key routes/services, `core/net.py`, media/web/model sync | Adds `roles.sensitive.assign`; API-key lifecycle/audit is one transaction. Operator private egress requires explicit CIDRs. Existing admin DNS TOCTOU is documented; no bot/answer-retention policy change. |
| Provider accounting and PR correctness | quota ledger/service, retrieval caches/service, agent/chat/evals/enrichment paths, model fallback, Retry-After/Evals/UI/E2E fixes | Non-null usage keys are unique and run-scoped. Completed calls commit through independent sessions before later fallible work. Query caches are organization-partitioned and publication-safe. |
| ACL projection race | document/pipeline Qdrant payloads, retrieval revision recheck, migration reprojection update | Existing indexed vector-backed documents move to `pending` until the reconciler stamps current revisions, causing a deliberate fail-closed availability interval after upgrade. |
| Dependency assurance | backend/frontend locks, `pyproject.toml`, pnpm overrides, `.github/workflows/audit.yml` | Transformers 5.15.1, js-yaml 4.3.2, Vitest 4.1.11 and PDF.js 5.4.624 are frozen. Runtime/build inventories stay separate; one exact no-release Accelerate exception has an executable expiry and removal condition. |

Migration `a31f6d8c9e02` adds `resource_reservations`,
`attachment_cleanup_jobs`, `chat_attachments.size_bytes` and the partial unique
usage-idempotency index. Cleanup jobs retain `user_id` and `size_bytes` without
FKs so allocation survives cascades. Historical attachment sizes are conservatively set to
the former 50 MiB ceiling; the migration does not inspect or delete storage.

## Benchmark, runtime and budget constraints

- No paid provider calls are authorized. Preserved ledger total is `$2.60807538` through the gateway, plus up to seven unresolved external rerank search units; reconciliation is read-only.
- The frozen anatomy dataset/questions and every prior result, including failed and invalidated runs, remain unchanged. Any future run requires a new manifest/result directory tied to this branch revision.
- RAGFlow, Onyx and LightRAG have no valid campaign score. Cache module latency is not authenticated cached-chat SSE latency.
- CAG production replay remains disabled. Its active development is outside this remediation branch unless a shared production defect requires coordination.

## Verification ledger

| Date | Revision/worktree | Command or review | Result | Scope and limits |
|---|---|---|---|---|
| 2026-09-08 | shared `/home/parshu/projects/ragz` | `git status --short --branch`; `git worktree list --porcelain` | Shared checkout confirmed dirty at `90bb3b1`; preserved. | Read-only baseline; no test claim. |
| 2026-09-08 | remote refs | `git fetch origin ...`; `gh pr view 11 --json ...` | `origin/main=9d08839`, PR #11 head=`3fac9fb`, only review-bot success reported. | No CI success claim. |
| 2026-09-08 | remediation worktree | `git rev-parse HEAD`; `git status --short --branch`; `git diff --check` | Clean isolated starting point at `3fac9fb`; diff check exited 0 before remediation edits. | Baseline only; functional tests pending. |
| 2026-09-08 | remediation worktree | Initial frontend `pnpm test` | 708 passed, 4 failed in document viewer security cases. | Failing-before evidence for sandbox/text/active/raster preview behavior. |
| 2026-09-08 | remediation worktree | Retained cache cancel reproducer | `embedding_dead_slot=True`, `expansion_dead_slot=True` before; both false after. | Deterministic publication-lock boundary, no provider/network. |
| 2026-09-08 | remediation worktree | Focused backend red regressions | Parallel quota requests both 201; tenant cache tests 2 failed; provider-body tests 3 failed; revision grounding and completed-call accounting tests failed. | Failures were captured before the respective patches; synthetic/fake providers only. |
| 2026-09-08 | remediation worktree | Focused backend integration batch | 205 passed, 9 warnings in 251.18s. | Preview/upload/parser/auth/cleanup/accounting/retrieval/migration paths with fresh Testcontainers. |
| 2026-09-08 | remediation worktree | Second-review regression batches | 96 passed in 50.83s; 89 passed in 26.39s; legacy migration 1 passed in 11.62s. | Covers pre-multipart cap, path parsers, raster bounds, revision probes, cache billing, detached idempotency and per-call provider accounting. |
| 2026-09-08 | remediation worktree | `ruff check .`; `mypy src`; `lint-imports`; `compileall`; `git diff --check` | Passed; 164 typed source files and 17 import contracts. | Application source typing; the repository's tests are not configured as a clean mypy target. |
| 2026-09-08 | remediation worktree | `pnpm test`; `pnpm typecheck`; `pnpm lint`; `pnpm build` | 106 files / 720 tests passed; typecheck, ESLint and production Vite build passed. | Existing React act/query warnings remain non-fatal; build reports one large-chunk warning. |
| 2026-09-08 | remediation worktree | Playwright `document-preview-security.spec.ts` | 3 passed. | Real Chromium executes harmless HTML/SVG attempts, escaped text and sandboxed PDF in the actual React viewer/hook harness. |
| 2026-09-08 | remediation worktree | Alembic migration suites | 17 passed plus dedicated legacy-size forward migration passed. | Fresh PostgreSQL only; linear head `a31f6d8c9e02`; no campaign DB. |
| 2026-09-08 | remediation worktree | Frozen dependency exports and audits | Backend runtime 218/full 258 requirements: no known vulnerabilities; frontend prod/full: zero advisories. | Linux/current interpreter markers; no blanket ignores. Runtime exports include FastAPI, Docling and Transformers; full includes Hatchling/Pytest. |
| 2026-09-08 | remediation worktree | `uv build --no-sources`; frontend Docker build; nginx `-t` | Wheel/sdist, frontend image build and nginx syntax passed. | Docker Scout image CVE scan was blocked by missing Docker login; backend image not built/scanned. |
| 2026-09-08 | remediation worktree | Broad backend run before second-review fixes | 1 failed, 1,837 passed, 14 skipped. | Not completion evidence: concurrent edits made it stale. Its sole `.xyz` parser failure is covered by the later 205-pass focused run. |
| 2026-09-08 | remediation worktree | Independent security/correctness review | Thirteen actionable observations; every confirmed item was patched and covered by focused regressions. | Review covered preview, admission, parsers, grounding revisions, cache cancellation/accounting, paid-call idempotency, image bounds, cleanup recovery, egress and blob cache state. A second broad rereview was stopped to conserve model usage after this completed review. |
| 2026-09-08 | remediation worktree | `DOCKER_HOST=unix:///var/run/docker.sock .venv/bin/python -m pytest -q` from `backend/` | **1,853 passed, 14 skipped, 10 warnings in 1,767.55s; exit 0.** | Definitive full backend run against modules imported from the isolated worktree; no paid providers or campaign stores. |
| 2026-09-09 | persistent remediation worktree | Independent cross-check completion edges | **2 failed before; 2 passed after** in 10.61s. | Real isolated PostgreSQL plus fake storage; proves pending cleanup quota and ambiguous commit compensation fixes. |
| 2026-09-09 | persistent remediation worktree | Affected backend integration group | **164 passed**, 7 warnings in 74.41s. | Cleanup/upload/migration, auth-adjacent agent accounting, expansion/embedding adapters and grounding. Imports verified from this worktree. |
| 2026-09-10 | persistent remediation worktree | Post-rereview backend group | **181 passed**, 7 warnings in 90.68s. | Adds server refresh-family replacement, real adapter failure accounting/malformed usage and single-call expansion wiring. |
| 2026-09-10 | persistent remediation worktree | First post-review full backend run | 1,871 passed, 14 skipped, **4 failed** in 1,043.98s. | Correctly caught unconditional login-family revocation breaking same-user session inventory/revocation; not completion evidence. |
| 2026-09-10 | persistent remediation worktree | Final `DOCKER_HOST=unix:///var/run/docker.sock .venv-cc/bin/python -m pytest -q` | **1,875 passed, 14 skipped, 10 warnings in 1,122.21s; exit 0.** | Definitive run after cross-user-only family replacement. Imports resolved from this checkout; fresh containers were removed and preserved services were not touched. |
| 2026-09-09 | persistent remediation worktree | `ruff`; `mypy src`; `lint-imports` | Passed; 164 source files and 17 contracts. | Static and architecture checks after CC patches. |
| 2026-09-10 | persistent remediation worktree | Frontend tests/type/lint/build and Playwright preview | **106 files / 725 tests; 3 browser tests; all checks/build passed.** | Vitest 4.1.11; dynamic PDF.js import; valid PDF canvas, active HTML/SVG and literal text. Exact Node 20.19 TypeScript/Vite build passes; one existing large-chunk warning. |
| 2026-09-10 | independent final rereview | CC-001 through CC-008 and follow-up session compatibility | **Ship; no Critical or Important findings remain.** | Reviewer verified cross-user password/OIDC fencing, same-user sessions, accounting/error boundaries, audit expiry, Node compatibility and grounding identity. |
| 2026-09-10 | PR #11 publication | Push, PR update, 15 inline replies/resolutions, CI inspection | Head updated; all 15 Cubic threads resolved. CI and dependency workflows are `action_required`. | Attempts to approve both fork workflows returned GitHub `403 Must have admin rights to Repository`; an upstream `marketcalls/ragz` admin must approve the runs. No remote CI pass is claimed. |
| 2026-09-09 | persistent remediation worktree | Frozen dependency audits | Frontend prod/full: no known vulnerabilities. Backend runtime 218/full 258: no known vulnerabilities, one exact ignored CVE each. | Accelerate CVE has no fixed release and a dated, reachability-reviewed exception; raw inventory is not described as all-clean. |

All confirmed in-scope audit, PR and cross-check application findings are fixed on this remediation branch.
CC-005 retains the exact no-patched-release Accelerate disposition described above.
GitHub-hosted CI remains pending the upstream-admin approval described in the
verification ledger; the completed local verification is not represented as CI.
The conditional product, deployment and image-assurance limits above remain explicit;
they are not represented as demonstrated application vulnerabilities. A second broad
rereview was stopped to conserve model usage after the completed independent review,
its 13 actionable observations, the corresponding fixes, and the definitive full
backend and frontend verification.
